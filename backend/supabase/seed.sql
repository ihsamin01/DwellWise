-- ─────────────────────────────────────────────────────────────────────────
-- Seed data for local development (`supabase db reset` runs this after
-- migrations). Keep it minimal; real demo content still lives in the app's
-- mock data (lib/data/*, lib/services/mock_search_service.dart) for now.
-- ─────────────────────────────────────────────────────────────────────────

-- TODO: insert a few demo profiles (tenant / owner / admin),
--       demo properties, and a sample chat once auth users exist locally.
